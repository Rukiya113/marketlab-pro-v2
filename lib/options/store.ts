import type {CanonicalInstrumentId} from '@/lib/market/events'; import type {OptionChainSnapshot} from './types'; const chains=new Map<CanonicalInstrumentId,OptionChainSnapshot>(); export const setOptionChain=(s:OptionChainSnapshot)=>void chains.set(s.underlyingId,s); export const getOptionChain=(id:CanonicalInstrumentId)=>chains.get(id)??null; export const emptyOptionChain=(id:CanonicalInstrumentId):OptionChainSnapshot=>({underlyingId:id,underlyingPrice:null,expiry:null,receivedAt:Date.now(),state:'OFFLINE',contracts:[]});
