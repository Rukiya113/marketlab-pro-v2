'use client';
import { useIntelligence } from '@/components/hooks';
import { useWorkstation } from '@/components/WorkstationProvider';
export default function OpportunitiesPage(){const{instrumentId}=useWorkstation();const state=useIntelligence(instrumentId);const o=state.opportunity;return <main className="modulePage"><section className="panel"><div className="panelTitle">OPPORTUNITY LIFECYCLE<span>{instrumentId}</span></div>{!o?<div className="xrayEmpty">No opportunity exists until ASURA produces a directional, strategy-backed candidate.</div>:<div className="asuraTwin"><section><h4>STATE</h4><p><span>Lifecycle</span><b>{o.state}</b></p><p><span>Score</span><b>{o.score}</b></p><p><span>Direction</span><b>{o.direction}</b></p><p><span>Strategy</span><b>{o.strategyId??'—'}</b></p></section><section><h4>TRADE MAP</h4><p><span>Entry zone</span><b>{o.entryZone?o.entryZone.map((x)=>x.toFixed(2)).join(' - '):'—'}</b></p><p><span>Invalidation</span><b>{o.invalidation?.toFixed(2)??'—'}</b></p><p><span>Targets</span><b>{o.targets.map((x)=>x.toFixed(2)).join(', ')||'—'}</b></p></section></div>}</section></main>}
