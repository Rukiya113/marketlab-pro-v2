import {NextRequest,NextResponse} from 'next/server'; import type {CanonicalInstrumentId} from '@/lib/market/events'; import {emptyOptionChain,getOptionChain} from '@/lib/options/store'; export const dynamic='force-dynamic'; export async function GET(r:NextRequest){const id=(r.nextUrl.searchParams.get('instrument')??'IN:NSE:INDEX:NIFTY50') as CanonicalInstrumentId;return NextResponse.json(getOptionChain(id)??emptyOptionChain(id),{headers:{'Cache-Control':'no-store'}})}
