'use client';
import { useIntelligence } from '@/components/hooks';
import { useWorkstation } from '@/components/WorkstationProvider';
export default function StrategyLabPage(){const{instrumentId}=useWorkstation();const{decision}=useIntelligence(instrumentId);return <main className="modulePage"><section className="panel"><div className="panelTitle">STRATEGY COMPETITION<span>{instrumentId}</span></div>{!decision?<div className="xrayEmpty">Waiting for ASURA intelligence.</div>:<div className="scannerTable"><div className="scannerRow scannerHead"><span>STRATEGY</span><span>SCORE</span><span>ELIGIBLE</span><span>REGIME</span><span>SETUP</span><span>EXECUTION</span><span>REASON</span></div>{decision.competition.evaluations.map((s)=><div className="scannerRow" key={s.id}><b>{s.label}</b><strong>{s.score}</strong><span>{s.eligible?'YES':'NO'}</span><span>{s.regimeCompatibility}</span><span>{s.setupScore}</span><span>{s.executionScore}</span><small>{s.reason}</small></div>)}</div>}</section></main>}
